export default function GameTogether({ socketRef, matchedPeer }) {
  return (
    <div className="p-4">
      <h2 className="text-xl font-bold mb-4">Game Zone 🎮</h2>
      <p>Select a game to play with your partner.</p>

      {/* later we add Tic Tac Toe / Chess / Trivia etc */}
    </div>
  );
}
